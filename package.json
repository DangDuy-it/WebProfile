{
  "name": "portfolio-fullstack",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"cd Profile_Frontend2 && npm run dev\" \"cd Profile_Backend && npm run dev\"",
    "dev:frontend": "npm run dev --prefix Profile_Frontend2",
    "dev:backend": "npm run dev --prefix Profile_Backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1",
    "react-icons-picker": "^1.0.9"
  }
}
